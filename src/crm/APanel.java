/*
 * This AScrollList used to create scrolable horisontal list of some frely disigned 
 * objects( JPanel compatible panels with free design). That allows us to create
 * such list's "records/fields" like at FireFox's "Downloads" window, w/ buttons,
 * progressbars and other.)
 * APanel extends JPanal but initialises by array of myObjects(extends 
 * Swing Objects of hi level such as JBttons).
 * 
 */
package crm;

/**
 *
 * @author kexital
 */
public class APanel {
    
}
